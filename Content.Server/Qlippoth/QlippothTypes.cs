namespace Content.Server.Qlippoth
{
    public enum PresenceType { Object, Event, Curse }
    public enum ObjectSize { Mob, Huge, Large, HandSized }
    public enum MovementType { Inanimate, Static, Animated, ConditionallyAnimated, NonConstrained, Specialist }
}
